import type {ConfigImage} from '..';

// const RATIO = 1.6;
const WIDTH = 640;
const HEIGHT = 360; // WIDTH / RATIO;

const WIDTH_HALF = WIDTH / 2;
const HEIGHT_HALF = HEIGHT / 2;

export default ({
    WIDTH,
    WIDTH_HALF,
    HEIGHT,
    HEIGHT_HALF,
}) as ConfigImage;
